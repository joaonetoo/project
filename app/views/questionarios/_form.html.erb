<%= form_for(@questionario) do |f| %>
  <% if @questionario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@questionario.errors.count, "error") %> prohibited this questionario from being saved:</h2>

      <ul>
      <% @questionario.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class = "form-group">
       <div class="col-lg-12">
          <%= f.label :titulo %><br>
          <%= f.text_field :titulo,class: "form-control" %> <br>
        </div>
    </div>

      <div class = "form-group">
       <div class="col-lg-12">
          <center><%= f.submit "Cadastrar", class:"btn btn-lg btn-warning"  %></center>
        </div>
      </div>
      </div>
<% end %>
