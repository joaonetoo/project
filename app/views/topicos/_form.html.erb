<%= form_for(@topico) do |f| %>
  <% if @topico.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topico.errors.count, "error") %> prohibited this topico from being saved:</h2>

      <ul>
      <% @topico.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class = "form-group">
     <div class="col-lg-4">
      <%= f.label :sessao_id %><br>
      <%= f.select :sessao_id, options_for_select(@sessoes.collect { |s| [s.sigla, s.id] }),{} ,class: "form-control" %>
    </div>
  </div>
    <div class = "form-group">
       <div class="col-lg-4">
          <%= f.label :sigla %><br>
          <%= f.text_field :sigla,class: "form-control" %>
        </div>
    </div>
    <div class = "form-group">
       <div class="col-lg-12">
          <%= f.label :descricao %><br>
          <%= f.text_area :descricao,class: "form-control",rows:4 %><br>
        </div>
    </div>
      <div class = "form-group">
       <div class="col-lg-12">
          <center><%= f.submit action, class:"btn btn-lg btn-warning"  %></center>
        </div>
      </div>
      </div>
<% end %>
